name: "Treffynnon's PowerShell scripting utilities"
on:
  release:
    types: [created]
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build manifest
      run: |
        $Tag = ${Env:GITHUB_REF} -Replace "refs/tags/",""
        .\src\generateManifest.ps1 -ModuleVersion $Tag
    - name: Lint manifest
      run: Test-ModuleManifest -Path .\src\TrUtilities.psd1
    - name: Publish Module
      run: Publish-Module -Path ".\src" -NuGetApiKey "${{ secrets.POWERSHELL_GALLERY_KEY }}" -Confirm -ErrorAction Stop -Force
